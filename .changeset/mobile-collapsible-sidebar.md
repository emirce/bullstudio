---
"bullstudio": patch
---

Make the dashboard sidebar collapsible on mobile devices using the native shadcn sidebar behavior, and derive the displayed dashboard version from the release package so it stays synchronized with published releases.
